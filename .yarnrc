# Hard-fail when the active Node/Yarn doesn't match the `engines` field in
# package.json. Prevents the silent "wrong node version → mysterious build
# failures" loop. Pinned versions live in .nvmrc, package.json `engines`,
# and the `volta` block — keep all four in sync.
--install.strict-semver true
engine-strict true
